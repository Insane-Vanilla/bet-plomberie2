import { Component } from '@angular/core';
import { Arguments } from '../arguments/arguments';

@Component({
  selector: 'app-about',
  imports: [Arguments],
  templateUrl: './about.html',
  styleUrl: './about.css',
})
export class About {}
